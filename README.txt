git status
git add <all the file paths given in git status>
git commit -m "message"
git push 

If you've already made changes. And If I have pushed chnages to the repository. Then use git stash. It temporarily stoires your changes.
After git stash, do git pull. Then do git stash pop.
